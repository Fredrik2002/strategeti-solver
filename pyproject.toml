[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "strategeti-solver"
version = "0.1.0"

[tool.setuptools]
py-modules = ["Strategeti", "Player", "PositionStorage"]

[tool.setuptools.packages.find]
where = ["."]